灰色(5)のブロックがいくつか含まれた H×W グリッド `g` が与えられます。
赤(2)のセルは固定された障害物で、その他は 0 です。

各灰色の連結成分をその形のまま平行移動し、上から 3 行以内に配置します。
ある位置に配置したときのスコアは、配置後にその成分の上下左右に接する赤セルの個数です。
赤セルと重なる位置には置けません (大きなペナルティのため)。
各成分について最もスコアが高い位置を求め、スコアの大きい成分から順に配置してください。
配置したセルは青(1)に変わり、元の位置は 0 になります。

結果のグリッドを出力してください。
