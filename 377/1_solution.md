# Task 377: 入れ子矩形の同心正方形化

## 問題

グリッドには複数の色で塗られた長方形が互いに内包する形で配置されている。
同じ色の長方形が複数存在する場合もある。外側から内側へ辿った順に色を並べ,
その順で同心正方形の層を構成したグリッドを出力せよ。

## ルール

1. 外側の長方形から順に内部の長方形を取り除いていくことで色の並びを得る。
2. 色の数を $n$ とする。出力は一辺 $(2(n-1)+1)$ の正方形グリッドとする。
3. 外側から順に幅 1 のリングを取得した色で塗りつぶす。最後の色は中心のセルを塗る。
