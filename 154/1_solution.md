# Task 154: 二つの枠の間へ 5 を移す

## 問題

グリッドには色`2`で出来た矩形の枠がちょうど 2 つ存在する。これらの枠は上下または左右に離れており、その間には空白の領域がある。
色`5`の連結成分が 2 つあり、それぞれは枠の外側で互いに反対の側に位置している。

各`5`の成分を、それが元々あった側から隙間の端に合わせるように隙間の中へ移動させる。その際、移動方向の軸に対して成分を反転（上下または左右）させる。元の位置の`5`は削除し、その他の色は変更しない。

## 出力

上記の操作を行った後のグリッドを返す。
