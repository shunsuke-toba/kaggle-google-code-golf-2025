# Task 271: 重み付き 3x3 パターン探索

## 問題

H×W グリッドが与えられる。各セルは 0〜9 の整数で色を表す。特に色 1 は青、色 8 は水色を表す。

グリッド内のすべての 3×3 領域を調べ、次のルールでスコアを計算する:

- 各青(1)セルにつき +9 点
- 各水色(8)セルにつき +1 点
- その他の色は 0 点

最もスコアが高い 3×3 領域をそのまま出力せよ。複数ある場合は最も上かつ左のものを選ぶ。

## 入出力

- 入力: `g` は整数の 2 次元リスト
- 出力: 最高スコアの 3×3 部分グリッド
