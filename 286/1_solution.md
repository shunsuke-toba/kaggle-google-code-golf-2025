# Task 286: Two-Color BFS Fill

## 問題

0(黒)と 8(水色)に加えて 2 種類の色が存在するグリッド`g`が与えられる。

非 0・非 8 のセルをすべて始点とし，キューから取り出したセルの 4 近傍にある 0 のセルを現在とは異なるもう一方の色で塗り，その座標をキューに追加する操作を繰り返す。

こうして得られたグリッドを出力せよ。8 のセルは壁として扱い，その他のセルは変更しない。

## 入力

- `g`: 整数の二次元配列。要素は 0,8,および 2 つの色のみ。

## 出力

- 操作後のグリッド。
