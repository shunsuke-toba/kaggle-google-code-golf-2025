# Task 148: 対向する柱を 8 で橋渡し

左右両端に同じ長さの`2`の縦列(柱)があり、片方の柱のいくつかの行には離れた位置に`8`が 1 つ存在する。

- `8`の行では、柱の`2`と`8`の間を`8`で埋め、元の`8`を`4`に置き換える。
- その行を柱の上から数えた順序で対応するもう一方の柱の行に、反対側の端から柱の直前まで`8`を描く(柱の`2`はそのまま)。

その他のセルは変化しない。
