# Task 165: Downward color propagation

## 問題

数字グリッド `g` が与えられる。0 は黒 (空白) を表す。上から順に走査し，左・右・上の三方向すべてに同じ色がある最初の色を基準色 `B` とする。

基準色の各セルについて，そのセルの直下から下方向に探索する。`B` 以外で 0 でない色 `C` を初めて見つけたなら，その列の基準色の直下から末端までを `C` で塗りつぶす。見つからなければ変更は行わない。

## 入力

- 0 以上 9 以下の整数からなる二次元配列 `g`。

## 出力

- 上記の変換を施した後の `g`。
