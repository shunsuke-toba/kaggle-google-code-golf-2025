# Task 101

グリッドは数字 0,1,2 で構成される。少なくとも 1 つ、`1`と`2`の両方を含む連結な基本図形が存在する。
他に同じ向きのまま整数倍に拡大された図形があり、そこでは `2` の位置だけが与えられており `1` が欠けている。
欠けている図形それぞれに対し、基本図形を拡大して重ね、対応する位置を `1` で塗りつぶす。既に存在する `2` はそのまま残す。図形がグリッドの外にはみ出す場合は、はみ出さない部分だけ描画する。

入力はグリッド `g`、出力も同じ大きさのグリッドである。
