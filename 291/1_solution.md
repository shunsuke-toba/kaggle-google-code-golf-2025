## 問題文

整数グリッド `g` が与えられます。以下の条件を満たすセルの色 `c` を求めてください。

- セル `(x,y)` の色を `c` とする。
- その右隣 `(x,y+1)` も `c`。
- その下隣 `(x+1,y)` も `c`。
- 右下 `(x+1,y+1)` は黒(0)。

このようなセルはちょうど 1 つ存在します。色 `c` を 1×1 の配列 `[[c]]` として出力してください。

## 入力

- 0〜9 の整数からなる二次元配列 `g`

## 出力

- `[[c]]`
