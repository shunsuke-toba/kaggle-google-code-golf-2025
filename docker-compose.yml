services:
  code-golf:
    build: .
    container_name: kaggle-code-golf
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true
    environment:
      - PYTHONPATH=/app
      - PYTHONUNBUFFERED=1
    command: bash
